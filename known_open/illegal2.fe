contract Foo:
   fn transfer(recipient: address) -> bool:
        return false
contract UniswapV2Pair:
    pub fn set_feeSto_setter(fee_to_setter: address):
        assert msg.sender < fee_to_setter, "UniwapV2: FORBIDDEN"
